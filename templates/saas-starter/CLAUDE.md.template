# CLAUDE.md — {{product_name_pascal}}

Generated by Ulak OS `/ulak-scaffold` on {{year}}. This file imports the Ulak OS core contract so every governance discipline applies from commit 1.

@../ulak.os/prompts/core/ulak-os-core-contract-2.0.0.md

Alternative (if Ulak OS is installed globally via its CLI):

@~/.ulak/core/ulak-os-core-contract-2.0.0.md

## Project identity

- Product: {{product_name_pascal}}
- Domain: {{product_domain}}
- Primary locale: {{locale_primary}}
- Supported locales: {{locales_json_array}}
- Payment: {{payment_provider}}
- Hosting: {{hosting}}
- Has reseller tier: {{has_reseller_tier}}

## Activated Ulak OS sector packs

Computed by `/ulak-scaffold` at generation time. Recorded in `reports/current/active-variables.yaml` when director first runs.

- `saas` (always active for any SaaS product)
- `{{product_domain}}` (if matches a known pack)
- `payment-integrated-saas` (if payment_provider != none)
- `reseller-enabled-saas` (if has_reseller_tier == true)
- `multi-tenant-supabase` (default when stack_backend = supabase)
- `vps-nginx-compose-topology` (if hosting = self-managed-vps)
- `greenfield-saas-starter` (this was a scaffold from Ulak — remove after first real director run)

## Activated rule packs

- `typescript-nextjs` (always for this stack)
- `docker-compose` (always for this stack)
- `api-security` (always for any HTTP surface)
- `turkish-locale` (if locale_primary = tr)
- `localization-ssot` (if len(locales_supported) >= 2)

## Runtime defaults (Ulak OS inherited)

- intent netse menü döngüsüne geri dönme
- artefakt zincirini erken başlat
- önce harita çıkar, sonra müdahale et
- customer/admin/public api ayrımını koru (see product-surface-split.md)
- validation koşmadan done deme

## Project-specific rules

Add here any rules specific to {{product_name}} that aren't covered by Ulak OS defaults. Examples:

- Build command: `pnpm build`
- Test command: `pnpm test`
- Lint command: `pnpm lint`
- Dev command: `pnpm dev`
- Deploy: triggered via GitHub Actions → webhook to `/deploy/webhook` on the VPS
- VPS host: [operator-managed — see `infrastructure/deploy.sh`]

## First director run

After the first real development work, run:

```
/director komple
```

This produces the v0.1.0 baseline audit under `reports/current/` with:

- Real manager-verdict (not the scaffolded placeholder)
- Evidence that commit 1 passes all relevant anti-pattern gates
- Pack-gap-register for any Ulak OS units the project would benefit from beyond what's already active

## Pattern imports

This project's `docs/governance/pattern-import-ledger.yaml` is seeded empty. Add entries when you import a pattern from another project per `docs/governance/pattern-import-ledger.md`.

## Canonical footer

Scaffolded from Ulak OS **v{{ulak_os_version}}** using `/ulak-scaffold` on {{year}}.
