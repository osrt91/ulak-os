// {{product_name_pascal}} — Admin sidebar shim
//
// Re-exports the shared dashboard sidebar with admin-role-scoped nav items.
// Actual primitive lives in components/dashboard/sidebar; this wrapper pins
// the role prop so pages don't need to remember to pass it.

import { DashboardSidebar } from "@/components/dashboard/sidebar"

export function AdminSidebar(props: Omit<React.ComponentProps<typeof DashboardSidebar>, "role">) {
  return <DashboardSidebar role="admin" {...props} />
}
